# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
.venv/
venv/
env/
ENV/
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# IDE / editor
.idea/
.vscode/
*.swp
*.swo

# OS metadata
Thumbs.db
.DS_Store
desktop.ini

# Legacy repos — preserved on disk for reference, not tracked
legacy/

# Report — LaTeX source + generated PDF (kept separately)
report/

# Claude / AI agent artifacts
.claude/
CLAUDE.md
CONVERSATION.md
_make_transcript.py
project_memory/

# Large model weights — download separately (see README)
server/weights/*.pt
server/weights/*.pth
server/weights/*.safetensors
server/weights/*.onnx
!server/weights/.gitkeep

# Runtime uploads + processed outputs
server/data/uploads/*
server/data/results/*
server/data/*.db
server/data/*.sqlite
server/data/*.sqlite3
server/data/*.log
server/data/dev_cert/
!server/data/uploads/.gitkeep
!server/data/results/.gitkeep

# Flutter (mobile placeholder)
mobile/

# Training artifacts — keep scripts, exclude data and runs
training/**/runs/
training/**/data/
training/**/*.pt

# Logs / coverage
*.log
.coverage
htmlcov/
